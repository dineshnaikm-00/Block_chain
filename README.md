
  # Content Moderation DAO

  This is a code bundle for Content Moderation DAO. The original project is available at https://www.figma.com/design/Yn1Cg7TkFqrLe73Hre20UK/Content-Moderation-DAO.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  